<?xml version="1.0" encoding="UTF-8"?>
<features>
    <repository>mvn:org.apache.camel.karaf/apache-camel/${camel.version}/xml/features</repository>

    <feature name='tif-example-jaxrs-jms-http' version='${pom.version}'>
        <feature version="${camel.version}">camel-cxf</feature>
        <feature version="${camel.version}">camel-jms</feature>
        <feature version="${camel.version}">camel-servlet</feature>
        <feature version="${camel.version}">camel-jetty</feature>
        <feature version="${camel.version}">camel-jaxb</feature>
        <feature version="${activemq.version}">activemq</feature>
        <feature>http</feature>
        
        <bundle>mvn:${pom.groupId}/jaxrs-jms-http-common/${pom.version}</bundle>
        <bundle>mvn:${pom.groupId}/jaxrs-jms-http-server/${pom.version}</bundle>
    </feature>

</features>